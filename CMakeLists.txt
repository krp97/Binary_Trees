set(CMAKE_BUILD_TYPE Debug)
cmake_minimum_required(VERSION 3.0)
project(Binary_Trees)
file(GLOB SOURCES "src/*.cpp")
add_executable(Binary_Trees ${SOURCES})
target_include_directories(Binary_Trees PRIVATE ${include})
